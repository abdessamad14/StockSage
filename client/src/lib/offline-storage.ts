// Database-only storage layer - completely replaces localStorage
// All operations go through API endpoints to SQLite database

// Re-export everything from database-storage to maintain compatibility
export * from './database-storage';
